CMAKE_MINIMUM_REQUIRED(VERSION 3.5)
PROJECT(SuperTemplate)

SET(ROSS_SOURCE "${CMAKE_CURRENT_SOURCE_DIR}/ROSS/")
SET(MODEL_SOURCE "${CMAKE_CURRENT_SOURCE_DIR}/src/")

ADD_SUBDIRECTORY(${ROSS_SOURCE})
ADD_SUBDIRECTORY(${MODEL_SOURCE})
